/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // No need to expose NEXT_PUBLIC_ variables here as they're automatically
    // exposed to the browser
};

module.exports = nextConfig; 